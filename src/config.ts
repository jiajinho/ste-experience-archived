import { Camera } from "types"

const cameras: Camera[] = [
  {
    position: [-10, 5, 5],
    lookAt: [0, 0, 0]
  },
  {
    position: [10, 5, 5],
    lookAt: [0, 0, 0]
  }
]

export default {
  cameras
}